spring:
  datasource:
    url: jdbc:mariadb://localhost:3307/spring-pf-2?createDatabaseIfNotExist=true
    username: root
    password: root
  jpa:
    hibernate:
      ddl-auto: update
      open-in-view: false
      show-sql: true